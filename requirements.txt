# Core
networkx>=3.2
rank-bm25>=0.2.2
numpy>=1.26
pydantic>=2.6

# API / dashboard
fastapi>=0.110
uvicorn[standard]>=0.29

# Local models (defaults): sentence-transformers for embeddings (CPU-only
# torch is fine); the LLM itself runs via a separate local Ollama server
# (https://ollama.com) reached over plain HTTP, so no LLM package is needed
# here. Both are guarded by try/except with automatic fallback in code, so
# the app still runs (with reduced quality) if this is left uninstalled.
sentence-transformers>=2.6

# Optional cloud integrations (guarded by try/except in code; safe to leave uninstalled)
neo4j>=5.19
anthropic>=0.34
openai>=1.30

# Dev / test
pytest>=8.0
httpx>=0.27
